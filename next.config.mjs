/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: "/breakpoint-homepage",
  output: "export",  // enables static exports
  reactStrictMode: true,
};

export default nextConfig;
